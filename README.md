# Northstar

Internal service for managing application configuration.

## Development

Python 3.11+

## Authentication

The service uses the internal authentication provider.

Session timeout is configurable through the application configuration.